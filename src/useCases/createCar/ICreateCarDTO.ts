export interface ICreateCarDTO {
  brand: string;
  model: string;
  // eslint-disable-next-line @typescript-eslint/naming-convention
  fab_date: string;
  price: number;
  color: string;
  userId: number;
}
